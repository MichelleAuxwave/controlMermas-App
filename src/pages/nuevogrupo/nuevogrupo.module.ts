import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { NuevogrupoPage } from './nuevogrupo';

@NgModule({
  declarations: [
    NuevogrupoPage,
  ],
  imports: [
    IonicPageModule.forChild(NuevogrupoPage),
  ],
})
export class NuevogrupoPageModule {}
